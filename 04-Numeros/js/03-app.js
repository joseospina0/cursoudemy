let resultado;

// funciones matematicas
resultado = Math.PI;
resultado = Math.round(2.8);
resultado = Math.round(2.5);
resultado = Math.round(2.1);

// redondear hacia abajo 
resultado = Math.floor(2.9);

// redondear hacia arriba 
resultado = Math.ceil(2.1);

// raiz cuadrada
resultado = Math.sqrt(144);

// valor absoluto
resultado = Math.abs(-500); 

//potencia 

resultado = Math.pow(8, 3);

// minimo
resultado = Math.min(3, 5, 1, 8, -3);   

// maximo
resultado = Math.max(3, 5, 1, 8, -3);

// aleatorio 
resultado = Math.random(); // devuelve un numero entre 0 y 1
resultado = Math.floor(Math.random() * 101); // devuelve un numero entre 0 y 100



console.log(resultado);