const random = document.querySelector('.btn-rndm');

/* random.addEventListener('click',  () => {
    let on = random.classList.contains('btn-active')
    if(on){
        random.classList.remove('btn-active')
    }else{
        random.classList.add('btn-active')
    }
}) */


// Ejemplo con 2 add even listener
/* random.addEventListener('mouseenter', añadirColor);

random.addEventListener('mouseleave', quitarColor);

function añadirColor(){
    random.classList.add('btn-active');
}

function quitarColor(){
    random.classList.remove('btn-active');
} */



