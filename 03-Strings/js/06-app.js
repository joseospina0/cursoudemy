const producto = "Monitor de 20 pulgadas"; 
const promocion = "en promocion ".repeat(3); // Repite el texto 3 veces
console.log(promocion); 
console.log(`${producto} ${promocion} !!!`);

const actividad = "Estoy aprendiendo JavaScript Moderno";
console.log(actividad.split(" ")); // Convierte el string en un arreglo, separando por espacios.