const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300, 
    disponible: true,
}

console.log(Object.keys(producto)); // Retorna propiedades

console.log(Object.values(producto)); // Restorna valores

console.log(Object.entries(producto)); // Retorna ambos pero por pares



